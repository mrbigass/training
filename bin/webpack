#!/usr/bin/env ruby

ENV['RAILS_ENV'] ||= ENV['RACK_ENV'] || 'development'
ENV['NODE_ENV']  ||= 'development'

def system!(*args)
  system(*args) || abort("\n== Command #{args} failed ==")
end

puts "\n== package install and start =="
system! 'yarn && yarn run webpack-dev-server'
